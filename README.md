# uConsole-shutdown
uConsole-shutdown is a zero-CPU background service fixing CM5 long-press shutdown issues. Features hardware auto-detect, 5s visual flash warning, and `ucs` CLI tool. uConsole-shutdown 是一個零耗能的背景服務，專為解決 CM5 長按關機失效所設計。具備硬體自動偵測、5秒螢幕閃爍警告，以及極簡的 ucs 設定工具。
